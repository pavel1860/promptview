from .model3 import Model


class RelationModel(Model):
    """
    Base class for models that represent a relationship (junction table)
    between two other models, optionally with metadata fields.
    """
    pass